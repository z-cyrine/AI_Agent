"""
Guide de démarrage rapide pour le projet IBN Agentic AI
========================================================

Ce fichier contient les instructions étape par étape pour démarrer le projet.
"""

# ============================================================================
# ÉTAPE 1: INSTALLATION
# ============================================================================

Installation de l'environnement:

1. Créer un environnement virtuel:
   ```
   python -m venv venv
   venv\Scripts\activate
   ```

2. Installer les dépendances:
   ```
   pip install -r requirements.txt
   ```

3. Configurer les variables d'environnement:
   ```
   copy .env.example .env
   ```
   
   Puis éditer .env et ajouter votre clé API:
   - Pour OpenAI: OPENAI_API_KEY=sk-...
   - Pour Anthropic: ANTHROPIC_API_KEY=sk-ant-...


# ============================================================================
# ÉTAPE 2: INGESTION DU CATALOGUE (Agent 2)
# ============================================================================

Option A - Avec OpenSlice (si vous avez une instance OpenSlice):
   ```
   python scripts/ingest_catalog.py
   ```

Option B - Mode Test (services fictifs):
   ```
   python scripts/ingest_catalog.py --mock
   ```

Vérification:
   La base ChromaDB sera créée dans: ./data/chroma_db/


# ============================================================================
# ÉTAPE 3: TEST DES AGENTS INDIVIDUELS
# ============================================================================

Test Agent 1 (Interpréteur):
   ```
   python -m agents.agent1_interpreter
   ```
   → Transforme le langage naturel en TMF921

Test Agent 2 (Sélecteur):
   ```
   python -m agents.agent2_selector
   ```
   → Recherche sémantique de services via RAG


# ============================================================================
# ÉTAPE 4: EXÉCUTION DU PIPELINE COMPLET
# ============================================================================

Exemple prédéfini (XR Application):
   ```
   python main.py --example
   ```

Requête personnalisée:
   ```
   python main.py --query "I need a low-latency 5G network for IoT devices"
   ```

Mode interactif:
   ```
   python main.py --interactive
   ```


# ============================================================================
# ÉTAPE 5: TESTS AVEC DIFFÉRENTES REQUÊTES
# ============================================================================

Exemples de requêtes à tester:

1. XR Application (du contexte):
   "I need three XR applications with 4 vCPUs each, interconnected with 5GB/s links and 5ms latency"

2. IoT Platform:
   "I need an IoT platform for smart city with low power devices and edge computing"

3. Video Streaming:
   "Deploy a 4K video streaming service with CDN for residential customers"

4. Edge Computing:
   "I need edge computing resources near Nice with low latency for real-time processing"

5. 5G Network Slice:
   "Create a 5G network slice with high throughput and low latency for mobile applications"


# ============================================================================
# STRUCTURE DU PROJET
# ============================================================================

ai_agent/
├── agents/
│   ├── agent1_interpreter.py  ✅ IMPLÉMENTÉ (Cyrine)
│   └── agent2_selector.py     ✅ IMPLÉMENTÉ (Cyrine)
├── schemas/
│   ├── tmf921.py              ✅ Schémas Intent
│   └── tmf641.py              📝 À utiliser par Sarra
├── scripts/
│   └── ingest_catalog.py      ✅ Ingestion catalogue
├── data/
│   └── chroma_db/             🗄️ Base vectorielle (généré)
├── config.py                  ✅ Configuration
├── main.py                    ✅ Pipeline Agents 1 & 2
└── requirements.txt           ✅ Dépendances


# ============================================================================
# PROCHAINES ÉTAPES (Pour Sarra)
# ============================================================================

Agent 3 - Traducteur (TMF641 Mapper):
   - Transformer Intent TMF921 → Service Order TMF641
   - Utiliser Few-Shot Prompting
   - Mapper les services sélectionnés vers les items d'ordre

Agent 4 - Validateur (Quality Assurance):
   - Valider le JSON TMF641 avec jsonschema
   - Vérifier la conformité des types
   - Boucle de correction avec Agent 3


# ============================================================================
# INTÉGRATION AVEC OPENSLICE
# ============================================================================

L'ordre de service TMF641 final sera envoyé à OpenSlice via:
   POST {OPENSLICE_BASE_URL}/tmf-api/serviceOrderingManagement/v4/serviceOrder

Headers:
   Authorization: Bearer {token}
   Content-Type: application/json

Body: ServiceOrder TMF641 (généré par Agent 3, validé par Agent 4)


# ============================================================================
# DÉPANNAGE
# ============================================================================

Problème: "Collection vide" lors du test Agent 2
   → Exécuter: python scripts/ingest_catalog.py --mock

Problème: "API key not found"
   → Vérifier le fichier .env et les clés API

Problème: "Connection refused to OpenSlice"
   → Utiliser le mode --mock pour travailler sans OpenSlice

Problème: "Module not found"
   → Vérifier que vous êtes dans l'environnement virtuel (venv)
   → Réinstaller: pip install -r requirements.txt


# ============================================================================
# RESSOURCES
# ============================================================================

Standards TMForum:
   - TMF921: https://www.tmforum.org/resources/specification/tmf921
   - TMF641: https://www.tmforum.org/resources/specification/tmf641
   - TMF633: https://www.tmforum.org/resources/specification/tmf633

LangChain Documentation:
   - https://python.langchain.com/docs/

ChromaDB Documentation:
   - https://docs.trychroma.com/
